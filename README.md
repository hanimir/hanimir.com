# hanimir.com

Personal site built with React + Vite.

## Development

```bash
npm install
npm run dev
```

## GitHub Pages

This site is configured for GitHub Pages with HashRouter and a production `base` of `/hanimir.com/`.

Build for deployment:

```bash
npm run build
```

Then publish `dist/` to GitHub Pages.
